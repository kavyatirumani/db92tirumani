 extends layout
 
block content
  h1= title
  br
  - var image_names = ["Bulldog.jpg","goldenretriever.jpg","panda.jpg","poodle.jpg","pug.jpg"];
  - for(let i=0; i<3; i++){
    - let number = Math.floor(Math.random()*5);
    img(src = `/images/${image_names[number]}` alt = `Image No ${image_names[number]}`)
    -}